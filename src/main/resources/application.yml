spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/school_mgt
    username: school_piti
    password: piti123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
